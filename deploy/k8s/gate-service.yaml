apiVersion: v1
kind: Service
metadata:
  name: gate-svc
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: gate
  ports:
    - name: tcp
      port: 10086
      targetPort: 10086
