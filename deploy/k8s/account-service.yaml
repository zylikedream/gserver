apiVersion: v1
kind: Service
metadata:
  name: account-svc
spec:
  type: NodePort
  selector:
    app: account
  ports:
    - name: http
      port: 18080
      targetPort: 18080
      nodePort: 30080
