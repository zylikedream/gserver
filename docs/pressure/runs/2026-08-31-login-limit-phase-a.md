# 压测记录：2026-08-31 login-limit-phase-a

> 状态：`通过`（限流分支验证；不是容量基线）
>
> 记录来源：2026-08-31 压测会话记录。以下只固化已确认的参数和观测值；未在会话记录中出现的字段标记为 `未记录`。

## 1. 实验定义

- **类型**：低阈值登录限流分支验证
- **目标**：在真实 bench 登录流量下确认登录限流、排队状态和断开原因指标能够触发并回落。
- **通过标准**：出现预期的限流结果；压测结束后并发许可和排队长度回到 0；不把该结果解释成系统容量。
- **范围外**：容量上限、稳定吞吐、p95/p99 延迟、长时间资源趋势。

## 2. 环境

| 项目 | 值 |
|---|---|
| Git commit | 未记录 |
| 分支 | 未记录 |
| 环境 | 本地临时压测环境 |
| 服务拓扑 | account + gate + 逻辑服务（具体节点拆分未记录） |
| Gate 地址 | 未记录 |
| Account prelogin | 未记录 |
| 数据状态 | 临时环境；压测结束后删除临时环境配置 |
| 机器规格 | 未记录 |

## 3. 服务端参数

本次使用低阈值临时覆盖：

```toml
[login_limit]
enabled = true
rate = 10.0
burst = 20
max_inflight = 5
queue_size = 10
wait_timeout = "1s"
```

## 4. 客户端参数

- bench bots：100
- 启动速率：50/s
- 运行时长：3–5 分钟范围内（会话记录未保留精确开始/结束时间）
- 账号前缀：未记录
- bot 类型、脚本、chat mixin：未记录
- 停止方式：未记录

## 5. 观测与证据

### 5.1 登录限流指标

会话记录中确认的结果：

```text
login_limit_total{result="ok"} 54
login_limit_total{result="rate_limited"} 244
login_inflight 0
login_queue_length 0
session_disconnect_total{reason="login_rate_limited"} 244
```

### 5.2 结果解释

- `rate_limited=244` 与 `session_disconnect_total{reason="login_rate_limited"}=244`，说明本次低阈值流量确实进入了限流拒绝路径。
- 压测结束时 `login_inflight=0`、`login_queue_length=0`，说明本次运行结束后没有残留许可或排队请求。
- 账号、角色、协议错误明细和资源曲线未记录，不能从本次数据推导业务容量或稳定性。

## 6. 结果

| 项目 | 结果 | 判定 |
|---|---:|---|
| 计划 bots | 100 | 已执行 |
| 登录成功 | 54 | 已观测 |
| 登录限流拒绝 | 244 | 已观测，符合预期 |
| 结束时 inflight | 0 | 通过 |
| 结束时 queue length | 0 | 通过 |
| 限流断开 | 244 | 已观测，与拒绝数一致 |
| 登录 p95/p99 | 未测 | 不用于本结论 |
| 容量上限 | 未测 | 不用于本结论 |

## 7. 结论

**结论：通过。**

本次验证了低阈值配置下登录限流拒绝分支和对应断开指标能够触发，并且压测结束后 `inflight` 与队列均归零。该运行只证明限流分支行为，不构成容量基线，也不能据此判断正常阈值下的最大登录 QPS。

## 8. 清理与后续

- 临时环境配置：已删除。
- 数据：本次数据兼容性不作为约束；具体清理范围未记录。
- 保留证据：本记录中的指标快照。
- 后续：使用唯一账号池，按 Smoke → Medium → Target → Saturation 分级运行；每个阶段创建独立记录，并补齐精确时间窗口、bench 输出、Prometheus/Grafana 查询、p95/p99 和资源曲线。
