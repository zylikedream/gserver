# GM 系统 MVP 实施计划（协议方案）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 通过游戏协议实现 GM 命令系统，客户端发 `ReqGMCommand` 执行命令，`ReqGMHelp` 查看帮助。

**Architecture:** GM 命令定义在独立的 `gm` 包中，方法注释通过 `go/doc` 提取为帮助文档，反射路由命令字符串到方法。`RoleGM` 作为 RoleModule 接收 proto 消息并委托 `gm` 包执行。Proto 消息 ID 范围 22001~22099。

**Tech Stack:** Go 1.25, protobuf (proto3), go/doc, reflect, gconv

---

## 文件结构

| 文件 | 操作 | 说明 |
|------|------|------|
| `protocol/client/gm.proto` | 新建 | Proto 消息定义 |
| `src/apps/role/internal/gm/gm.go` | 新建 | GM 命令定义 + go/doc 帮助提取 + 反射路由 |
| `src/apps/role/internal/logic/role_gm.go` | 新建 | RoleGM 模块 |
| `src/apps/role/internal/logic/role_main.go` | 修改 | roleModules 加 `GM *RoleGM` |

---

### Task 1: 创建 Proto 消息定义

**Files:**
- Create: `protocol/client/gm.proto`

- [ ] **Step 1: 创建 gm.proto 文件**

```proto
// ID: 22001~22099
syntax = "proto3";
option go_package="./pb;pb";
package galaxy.protocol;

import "msg_options.proto";

message ReqGMCommand {
    option (msg_id) = 22001;
    string cmd = 1;
}

message RspGMCommand {
    option (msg_id) = 22002;
    string result = 1;
}

message ReqGMHelp {
    option (msg_id) = 22003;
}

message RspGMHelp {
    option (msg_id) = 22004;
    repeated PCmdDoc commands = 1;
}

message PCmdDoc {
    string name = 1;
    string brief = 2;
    string usage = 3;
    string example = 4;
}
```

- [ ] **Step 2: 生成 Go 代码**

Run: `cd /home/zyr/workspace/gserver && make pb`
Expected: 生成 `protocol/pb/gm.pb.go`，无错误

- [ ] **Step 3: 验证生成的代码**

Run: `cd /home/zyr/workspace/gserver && go build ./protocol/pb/`
Expected: 编译通过

- [ ] **Step 4: Commit**

```bash
git add protocol/client/gm.proto protocol/pb/gm.pb.go
git commit -m "feat(gm): add proto messages for GM commands"
```

---

### Task 2: 创建 GM 命令模块 (gm.go)

**Files:**
- Create: `src/apps/role/internal/gm/gm.go`

- [ ] **Step 1: 创建 gm.go 文件**

```go
package gm

import (
	"context"
	"fmt"
	"go/doc"
	"go/parser"
	"go/token"
	"os"
	"path/filepath"
	"reflect"
	"runtime"
	"strings"
	"unicode"

	"gserver/src/apps/role/internal/logic"
	"gserver/src/apps/role/internal/logic/bag"

	"github.com/gogf/gf/v2/util/gconv"
)

// CmdDoc 命令文档
type CmdDoc struct {
	Name    string
	Brief   string
	Usage   string
	Example string
}

// GM 命令处理器，持有 RoleMain 引用，在 actor 上下文内执行
type GM struct {
	role *logic.RoleMain
	ctx  context.Context
}

func NewGM(ctx context.Context, role *logic.RoleMain) *GM {
	return &GM{role: role, ctx: ctx}
}

// ========== 帮助提取 ==========

var (
	cmdDocs []CmdDoc
	cmdMap  map[string]reflect.Method
	inited  bool
)

func ensureInited() {
	if inited {
		return
	}
	inited = true
	cmdDocs = extractDocs()
	cmdMap = buildCmdMap()
}

// extractDocs 用 go/doc 解析 gm.go 源文件提取命令文档
func extractDocs() []CmdDoc {
	_, filePath, _, _ := runtime.Caller(0)
	dir := filepath.Dir(filePath)
	filename := filepath.Base(filePath)

	fset := token.NewFileSet()
	pkgs, err := parser.ParseDir(fset, dir, func(fi os.FileInfo) bool {
		return fi.Name() == filename
	}, parser.ParseComments)
	if err != nil {
		return nil
	}

	for _, pkg := range pkgs {
		p := doc.New(pkg, "./", doc.AllDocs)
		for _, t := range p.Types {
			if t.Name != "GM" {
				continue
			}
			var docs []CmdDoc
			for _, m := range t.Methods {
				if !m.Decl.Name.IsExported() {
					continue
				}
				cmdName := camelToSnake(m.Name)
				d := parseCmdDoc(cmdName, m.Doc)
				docs = append(docs, d)
			}
			return docs
		}
	}
	return nil
}

func parseCmdDoc(name string, docStr string) CmdDoc {
	d := CmdDoc{Name: name}
	lines := strings.Split(docStr, "\n")
	for _, line := range lines {
		line = strings.TrimSpace(line)
		if line == "" {
			continue
		}
		if d.Brief == "" {
			d.Brief = line
		}
		if strings.HasPrefix(line, "用法:") || strings.HasPrefix(line, "用法：") {
			d.Usage = strings.TrimSpace(strings.TrimPrefix(strings.TrimPrefix(line, "用法:"), "用法："))
		}
		if strings.HasPrefix(line, "示例:") || strings.HasPrefix(line, "示例：") {
			d.Example = strings.TrimSpace(strings.TrimPrefix(strings.TrimPrefix(line, "示例:"), "示例："))
		}
	}
	return d
}

// buildCmdMap 构建命令名到 reflect.Method 的映射
func buildCmdMap() map[string]reflect.Method {
	m := make(map[string]reflect.Method)
	t := reflect.TypeFor[*GM]()
	for i := 0; i < t.NumMethod(); i++ {
		method := t.Method(i)
		if !method.IsExported() {
			continue
		}
		cmdName := camelToSnake(method.Name)
		m[cmdName] = method
	}
	return m
}

func camelToSnake(s string) string {
	var result []rune
	for i, r := range s {
		if unicode.IsUpper(r) {
			if i > 0 {
				result = append(result, '_')
			}
			result = append(result, unicode.ToLower(r))
		} else {
			result = append(result, r)
		}
	}
	return string(result)
}

// GetCmdDocs 返回命令文档列表
func GetCmdDocs() []CmdDoc {
	ensureInited()
	return cmdDocs
}

// ========== 命令路由 ==========

// ExecCommand 执行 GM 命令
func (g *GM) ExecCommand(name string, args []string) error {
	ensureInited()
	method, ok := cmdMap[name]
	if !ok {
		return fmt.Errorf("unknown command: %s", name)
	}
	// 构建参数：receiver + args...
	in := make([]reflect.Value, 1+len(args))
	in[0] = reflect.ValueOf(g)
	for i, arg := range args {
		paramType := method.Type.In(i + 1)
		val := gconv.Convert(arg, paramType.Kind().String())
		in[i+1] = reflect.ValueOf(val).Convert(paramType)
	}
	results := method.Func.Call(in)
	if len(results) > 0 {
		if err, ok := results[0].Interface().(error); ok && err != nil {
			return err
		}
	}
	return nil
}

// ========== GM 命令定义 ==========
// 每个命令方法的注释即为帮助文档，格式：
// 第一行：简要说明
// 用法: 命令名 [参数...]
// 示例: 命令名 参数值...

// AddGoods 添加物品或货币
// 用法: add_goods [物品ID] [数量]
// 示例: add_goods 1001 10
func (g *GM) AddGoods(itemID int, num uint64) error {
	return g.role.Bag.AddItem(g.ctx, []bag.Item{
		{ID: itemID, Num: num},
	})
}

// RemoveGoods 移除物品或货币
// 用法: remove_goods [物品ID] [数量]
// 示例: remove_goods 1001 5
func (g *GM) RemoveGoods(itemID int, num uint64) error {
	return g.role.Bag.DecItem(g.ctx, []bag.Item{
		{ID: itemID, Num: num},
	})
}
```

- [ ] **Step 2: 验证编译**

Run: `cd /home/zyr/workspace/gserver && go build ./src/apps/role/internal/gm/`
Expected: 编译通过

- [ ] **Step 3: Commit**

```bash
git add src/apps/role/internal/gm/gm.go
git commit -m "feat(gm): add GM command module with go/doc help and reflection routing"
```

---

### Task 3: 创建 RoleGM 模块

**Files:**
- Create: `src/apps/role/internal/logic/role_gm.go`

- [ ] **Step 1: 创建 role_gm.go 文件**

RoleGM 遵循 RoleModule 模式（和 RoleExtra 类似，无持久化状态）：
- 嵌入 `RoleModule`
- `PersistState()` 返回 `nil`（不需要持久化）
- 通过 `r.Role` 访问 RoleMain 及其他模块

Handler 方法签名：`func (r *RoleGM) ReqXxx(ctx context.Context, req *pb.ReqXxx) (*pb.RspXxx, error)`

```go
package logic

import (
	"context"

	"gserver/protocol/pb"
	"gserver/src/apps/role/internal/gm"
)

type RoleGM struct {
	RoleModule
}

var _ IRoleModule = (*RoleGM)(nil)

func (r *RoleGM) ReqGMCommand(ctx context.Context, req *pb.ReqGMCommand) (*pb.RspGMCommand, error) {
	parts := strings.Fields(req.Cmd)
	if len(parts) == 0 {
		return &pb.RspGMCommand{Result: "empty command"}, nil
	}
	g := gm.NewGM(ctx, r.Role)
	err := g.ExecCommand(parts[0], parts[1:])
	if err != nil {
		return &pb.RspGMCommand{Result: err.Error()}, nil
	}
	return &pb.RspGMCommand{Result: "ok"}, nil
}

func (r *RoleGM) ReqGMHelp(ctx context.Context, req *pb.ReqGMHelp) (*pb.RspGMHelp, error) {
	docs := gm.GetCmdDocs()
	cmds := make([]*pb.PCmdDoc, 0, len(docs))
	for _, d := range docs {
		cmds = append(cmds, &pb.PCmdDoc{
			Name:    d.Name,
			Brief:   d.Brief,
			Usage:   d.Usage,
			Example: d.Example,
		})
	}
	return &pb.RspGMHelp{Commands: cmds}, nil
}
```

> **注意：** `strings.Fields` 需要导入 `"strings"` 包。最终 import 块：

```go
import (
	"context"
	"strings"

	"gserver/protocol/pb"
	"gserver/src/apps/role/internal/gm"
)
```

- [ ] **Step 2: 验证编译**

Run: `cd /home/zyr/workspace/gserver && go build ./src/apps/role/internal/logic/`
Expected: 编译通过（此时 RoleGM 还未被 roleModules 引用，但包本身应编译通过）

- [ ] **Step 3: Commit**

```bash
git add src/apps/role/internal/logic/role_gm.go
git commit -m "feat(gm): add RoleGM module with ReqGMCommand and ReqGMHelp handlers"
```

---

### Task 4: 注册 RoleGM 到 roleModules

**Files:**
- Modify: `src/apps/role/internal/logic/role_main.go:62-67`

- [ ] **Step 1: 在 roleModules 结构体添加 GM 字段**

修改 `src/apps/role/internal/logic/role_main.go` 第 62-67 行的 `roleModules` 结构体：

```go
// 原代码（第 62-67 行）：
type roleModules struct {
	Bag    *RoleBag
	Basic  *RoleBasic
	Public *RolePublic
	Extra  *RoleExtra
}

// 改为：
type roleModules struct {
	Bag    *RoleBag
	Basic  *RoleBasic
	Public *RolePublic
	Extra  *RoleExtra
	GM     *RoleGM
}
```

`initRoleModules` 方法（第 140-159 行）通过反射遍历 `roleModules` 的导出指针字段并自动初始化，只要字段类型实现 `IRoleModule` 接口。`RoleGM` 嵌入了 `RoleModule` 并通过 `var _ IRoleModule = (*RoleGM)(nil)` 验证了接口实现，所以不需要修改 `initRoleModules`。

同样，`initMsgHandler`（第 278-283 行）遍历所有模块调用 `AddMsgHandler`，会自动注册 `RoleGM` 的 `ReqGMCommand` 和 `ReqGMHelp` handler。

- [ ] **Step 2: 验证编译**

Run: `cd /home/zyr/workspace/gserver && go build ./src/apps/role/...`
Expected: 编译通过

- [ ] **Step 3: Commit**

```bash
git add src/apps/role/internal/logic/role_main.go
git commit -m "feat(gm): register RoleGM in roleModules"
```

---

### Task 5: 编写功能文档

**Files:**
- Create: `docs/system/gm.md`

- [ ] **Step 1: 创建 GM 系统文档**

```markdown
# GM 系统

## 概述

GM（Game Master）命令系统，通过游戏协议提供开发测试命令。命令定义在 `gm` 包中，方法注释即为帮助文档，通过 `go/doc` 自动提取。

## 协议接口

### 执行命令

消息 ID: 22001 (请求) / 22002 (响应)

请求：
```proto
message ReqGMCommand {
    string cmd = 1;  // 命令字符串，如 "add_goods 1001 10"
}
```

响应：
```proto
message RspGMCommand {
    string result = 1;  // 执行结果，成功为 "ok"
}
```

### 查看帮助

消息 ID: 22003 (请求) / 22004 (响应)

请求：
```proto
message ReqGMHelp {}
```

响应：
```proto
message RspGMHelp {
    repeated PCmdDoc commands = 1;
}
```

## 命令列表

| 命令 | 说明 | 用法 |
|------|------|------|
| add_goods | 添加物品或货币 | add_goods [物品ID] [数量] |
| remove_goods | 移除物品或货币 | remove_goods [物品ID] [数量] |

新增命令只需在 `gm.go` 中添加导出方法并写好注释即可，无需改动其他文件。

## 架构

```
客户端发 ReqGMCommand {cmd: "add_goods 1001 10"}
  → Gateway 转发到 RoleMain
  → HandleClientMsg → MsgHandler 路由到 RoleGM.ReqGMCommand
  → gm.NewGM(ctx, r.Role).ExecCommand("add_goods", ["1001", "10"])
  → 反射查找 add_goods → GM.AddGoods(1001, 10)
  → role.Bag.AddItem(...)
  → 返回 RspGMCommand {result: "ok"}
```

## 代码位置

| 文件 | 说明 |
|------|------|
| `protocol/client/gm.proto` | Proto 消息定义 |
| `src/apps/role/internal/gm/gm.go` | GM 命令定义 + 帮助提取 + 反射路由 |
| `src/apps/role/internal/logic/role_gm.go` | RoleGM 模块 |
| `src/apps/role/internal/logic/role_main.go` | roleModules 注册 |

## 设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 传输协议 | 游戏协议 | 复用现有 gateway→role 管道 |
| 命令定义 | 独立 gm 包 | go/doc 只提取命令方法 |
| 执行上下文 | RoleModule 内 | 和 RoleBag 一致，直接访问其他模块 |
| 帮助文档 | go/doc 提取 | 注释写一处，避免冗余 |
| 鉴权 | 不做 | MVP 阶段 |
```

- [ ] **Step 2: Commit**

```bash
git add docs/system/gm.md
git commit -m "docs: GM 系统功能文档"
```

---

## 验证

编译通过后，可以通过以下方式验证：

1. 启动服务器，登录一个角色
2. 发送 `ReqGMCommand {cmd: "add_goods 1001 10"}` 应返回 `RspGMCommand {result: "ok"}`
3. 发送 `ReqGMHelp {}` 应返回包含 `add_goods` 和 `remove_goods` 的命令列表
4. 检查背包物品是否正确添加/移除
