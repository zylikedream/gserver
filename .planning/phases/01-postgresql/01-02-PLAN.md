---
phase: 01-postgresql
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/role/internal/logic/role_schema.go
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
user_setup: []

must_haves:
  truths:
    - "6 个角色模块表创建成功"
    - "每个表都有 role_id 主键"
    - "每个表都有 update_at 索引"
    - "表结构与现有 MongoDB 字段映射正确"
  artifacts:
    - path: "apps/role/internal/logic/role_schema.go"
      provides: "6 个角色模块的 PostgreSQL 表定义"
      min_lines: 150
      contains: "role_basic", "role_bag", "role_sign", "role_activity", "role_account", "role_extra"
  key_links:
    - from: "role_schema.go"
      to: "core/gxypgx/schema.go"
      via: "调用 CreateTable/CreateIndex 方法"
      pattern: "PGX().CreateTable"
    - from: "role_schema.go"
      to: "apps/role/internal/logic/role_*.go"
      via: "参考现有状态结构体定义字段"
      pattern: "RoleBasicState|RoleBagState|RoleSignState"
---

<objective>
创建 6 个角色模块的 PostgreSQL 表结构

目的：将角色模块的 6 个状态表从 MongoDB 设计转换为 PostgreSQL 关系型表结构，包含主键和索引。

输出：
- `role_schema.go` 文件，包含 6 个表的 CREATE TABLE 和 CREATE INDEX 语句
- role_basic（角色基础信息）
- role_bag（背包物品）
- role_sign（签到记录）
- role_activity（活动参与）
- role_account（账号角色映射）
- role_extra（扩展数据）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-postgresql/01-CONTEXT.md

# 参考现有状态结构
@apps/role/internal/logic/role_basic.go
@apps/role/internal/logic/role_bag.go
@apps/role/internal/logic/role_sign.go
@apps/role/internal/logic/role_activity.go

# 依赖的上游计划
@.planning/phases/01-postgresql/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 role_schema.go 文件定义表结构</name>
  <files>apps/role/internal/logic/role_schema.go</files>
  <read_first>
    apps/role/internal/logic/role_basic.go - 参考 RoleBasicState 字段
    apps/role/internal/logic/role_bag.go - 参考 RoleBagState 字段
    apps/role/internal/logic/role_sign.go - 参考 RoleSignState 字段
    apps/role/internal/logic/role_activity.go - 参考 RoleActivityPersistState 字段
    core/gxypgx/schema.go - 需要调用 CreateTable/CreateIndex 方法
  </read_first>
  <action>
创建 `apps/role/internal/logic/role_schema.go` 文件，定义 6 个角色模块表的创建语句。

**文件结构：**
```go
package logic

import (
    "context"
    "gserver/core/gxypgx"
    "github.com/gogf/gf/v2/os/glog"
)

// InitRoleSchema 初始化角色模块的数据库表
func InitRoleSchema(ctx context.Context) {
    pgx := gxypgx.PGX()

    // 1. role_basic 表
    if err := createRoleBasicTable(ctx, pgx); err != nil {
        glog.Fatal(ctx, err)
    }

    // 2. role_bag 表
    if err := createRoleBagTable(ctx, pgx); err != nil {
        glog.Fatal(ctx, err)
    }

    // 3. role_sign 表
    if err := createRoleSignTable(ctx, pgx); err != nil {
        glog.Fatal(ctx, err)
    }

    // 4. role_activity 表
    if err := createRoleActivityTable(ctx, pgx); err != nil {
        glog.Fatal(ctx, err)
    }

    // 5. role_account 表
    if err := createRoleAccountTable(ctx, pgx); err != nil {
        glog.Fatal(ctx, err)
    }

    // 6. role_extra 表
    if err := createRoleExtraTable(ctx, pgx); err != nil {
        glog.Fatal(ctx, err)
    }

    glog.Info(ctx, "[schema] all role tables created successfully")
}

// createRoleBasicTable 创建角色基础信息表
func createRoleBasicTable(ctx context.Context, pgx *gxypgx.PGXApp) error {
    sql := `
    CREATE TABLE IF NOT EXISTS role_basic (
        role_id BIGINT PRIMARY KEY,
        role_name VARCHAR(64) NOT NULL,
        head VARCHAR(128),
        login_tm TIMESTAMPTZ,
        logout_tm TIMESTAMPTZ,
        create_tm TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        vip_lv INT NOT NULL DEFAULT 0,
        update_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    )`

    if err := pgx.CreateTable(ctx, sql); err != nil {
        return err
    }

    // 创建 update_at 索引
    indexSQL := `
    CREATE INDEX IF NOT EXISTS idx_role_basic_update_at
    ON role_basic(update_at)
    `
    return pgx.CreateIndex(ctx, indexSQL)
}

// createRoleBagTable 创建背包物品表
func createRoleBagTable(ctx context.Context, pgx *gxypgx.PGXApp) error {
    sql := `
    CREATE TABLE IF NOT EXISTS role_bag (
        role_id BIGINT PRIMARY KEY,
        items JSONB NOT NULL DEFAULT '{}',
        currencies JSONB NOT NULL DEFAULT '{}',
        grid_use INT NOT NULL DEFAULT 0,
        update_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    )`

    if err := pgx.CreateTable(ctx, sql); err != nil {
        return err
    }

    indexSQL := `
    CREATE INDEX IF NOT EXISTS idx_role_bag_update_at
    ON role_bag(update_at)
    `
    return pgx.CreateIndex(ctx, indexSQL)
}

// createRoleSignTable 创建签到记录表
func createRoleSignTable(ctx context.Context, pgx *gxypgx.PGXApp) error {
    sql := `
    CREATE TABLE IF NOT EXISTS role_sign (
        role_id BIGINT PRIMARY KEY,
        draw_time TIMESTAMPTZ,
        sign_day INT NOT NULL DEFAULT 0,
        accum_draw_stage INT[] NOT NULL DEFAULT '{}',
        draw_day INT NOT NULL DEFAULT 0,
        patch INT NOT NULL DEFAULT 0,
        update_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    )`

    if err := pgx.CreateTable(ctx, sql); err != nil {
        return err
    }

    indexSQL := `
    CREATE INDEX IF NOT EXISTS idx_role_sign_update_at
    ON role_sign(update_at)
    `
    return pgx.CreateIndex(ctx, indexSQL)
}

// createRoleActivityTable 创建活动参与表
func createRoleActivityTable(ctx context.Context, pgx *gxypgx.PGXApp) error {
    sql := `
    CREATE TABLE IF NOT EXISTS role_activity (
        role_id BIGINT PRIMARY KEY,
        activitys JSONB NOT NULL DEFAULT '{}',
        update_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    )`

    if err := pgx.CreateTable(ctx, sql); err != nil {
        return err
    }

    indexSQL := `
    CREATE INDEX IF NOT EXISTS idx_role_activity_update_at
    ON role_activity(update_at)
    `
    return pgx.CreateIndex(ctx, indexSQL)
}

// createRoleAccountTable 创建账号角色映射表
func createRoleAccountTable(ctx context.Context, pgx *gxypgx.PGXApp) error {
    sql := `
    CREATE TABLE IF NOT EXISTS role_account (
        role_id BIGINT PRIMARY KEY,
        account_id BIGINT NOT NULL,
        server_id INT NOT NULL,
        update_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        UNIQUE(account_id, server_id)
    )`

    if err := pgx.CreateTable(ctx, sql); err != nil {
        return err
    }

    // update_at 索引
    indexSQL1 := `
    CREATE INDEX IF NOT EXISTS idx_role_account_update_at
    ON role_account(update_at)
    `
    if err := pgx.CreateIndex(ctx, indexSQL1); err != nil {
        return err
    }

    // account_id + server_id 联合索引（用于按账号查询角色）
    indexSQL2 := `
    CREATE INDEX IF NOT EXISTS idx_role_account_account_server
    ON role_account(account_id, server_id)
    `
    return pgx.CreateIndex(ctx, indexSQL2)
}

// createRoleExtraTable 创建扩展数据表
func createRoleExtraTable(ctx context.Context, pgx *gxypgx.PGXApp) error {
    sql := `
    CREATE TABLE IF NOT EXISTS role_extra (
        role_id BIGINT PRIMARY KEY,
        extra_data JSONB NOT NULL DEFAULT '{}',
        update_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    )`

    if err := pgx.CreateTable(ctx, sql); err != nil {
        return err
    }

    indexSQL := `
    CREATE INDEX IF NOT EXISTS idx_role_extra_update_at
    ON role_extra(update_at)
    `
    return pgx.CreateIndex(ctx, indexSQL)
}
```

**MongoDB → PostgreSQL 字段映射规则：**

1. **基础类型映射：**
   - `string` → `VARCHAR(n)` 或 `TEXT`
   - `int` → `INT`
   - `int64` → `BIGINT`
   - `time.Time` → `TIMESTAMPTZ`（带时区时间戳）
   - `bool` → `BOOLEAN`

2. **复杂类型映射：**
   - `map[int]*BagItem` → `JSONB`（PostgreSQL 原生 JSON 支持）
   - `map[int32]ActivityData` → `JSONB`
   - `[]int` → `INT[]`（PostgreSQL 数组类型）

3. **通用字段：**
   - 所有表都有 `role_id BIGINT PRIMARY KEY`（主键）
   - 所有表都有 `update_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`（更新时间）

4. **特殊处理：**
   - `role_account` 表添加 `UNIQUE(account_id, server_id)` 约束（一个账号在同一服务器只能有一个角色）
   - `bson:"inline"` 展开的字段（RoleID, UpdateAt, Version）直接作为表字段
   - `hash:"-"` 标签的字段在 PostgreSQL 中保留（不再需要 hash 检测）

**Go 代码内建表创建（D-04）：**
- 使用 `CREATE TABLE IF NOT EXISTS` 避免重复创建
- 使用 `CREATE INDEX IF NOT EXISTS` 避免重复创建索引
- 启动时调用 `InitRoleSchema(ctx)` 自动创建表

**分列存储设计（D-05）：**
- 每个模块一张表（role_basic, role_bag, role_sign, role_activity, role_account, role_extra）
- 表结构采用关系型设计（明确的字段类型和约束）
- 复杂嵌套结构使用 JSONB 存储（如 items, activitys）
  </action>
  <verify>
    <automated>test -f apps/role/internal/logic/role_schema.go && grep -q "CREATE TABLE IF NOT EXISTS role_basic" apps/role/internal/logic/role_schema.go && grep -q "CREATE TABLE IF NOT EXISTS role_bag\|role_sign\|role_activity\|role_account\|role_extra" apps/role/internal/logic/role_schema.go</automated>
  </verify>
  <done>
    - role_schema.go 文件创建完成
    - 定义 6 个表的 CREATE TABLE 语句
    - 定义每个表的 update_at 索引
    - role_account 表包含 UNIQUE 约束
    - 提供 InitRoleSchema 初始化函数
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 role app 启动时调用 InitRoleSchema</name>
  <files>apps/role/role.go</files>
  <read_first>
    apps/role/role.go - 查找 role app 的启动逻辑
    apps/role/internal/logic/role_schema.go - 确认 InitRoleSchema 函数签名
  </read_first>
  <action>
在 role app 的启动流程中调用 `InitRoleSchema(ctx)`，确保数据库表在应用启动时创建。

**实现步骤：**

1. **查找 role app 的主文件：**
   ```bash
   find /home/zyr/workspace/gserver/apps/role -name "role.go" -o -name "main.go" -o -name "app.go"
   ```

2. **在启动流程中添加表初始化：**
   假设文件为 `apps/role/role.go`，在 `OnModStart` 或 `OnModStartAfter` 方法中调用：

   ```go
   func (r *RoleApp) OnModStart(ctx context.Context) error {
       // ... 现有启动逻辑 ...

       // 初始化 PostgreSQL 表
       logic.InitRoleSchema(ctx)

       // ... 其他逻辑 ...
       return nil
   }
   ```

3. **如果 role app 没有单独的启动文件，** 则在 `apps/role/internal/logic/role_main.go` 中添加：

   ```go
   func (r *RoleMain) OnModStart(ctx context.Context) error {
       // ... 现有启动逻辑 ...

       // 初始化 PostgreSQL 表
       InitRoleSchema(ctx)

       // ... 其他逻辑 ...
       return nil
   }
   ```

**关键点：**
- 在 PostgreSQL 连接建立之后调用（依赖 01-01 计划的连接初始化）
- 使用快速失败策略（InitRoleSchema 内部已使用 glog.Fatal）
- 确保只执行一次（不要在多个模块中重复调用）
  </action>
  <verify>
    <automated>grep -rn "InitRoleSchema" apps/role/ --include="*.go"</automated>
  </verify>
  <done>
    - role app 启动时调用 InitRoleSchema
    - 表初始化在 PostgreSQL 连接建立后执行
    - 启动日志显示 "all role tables created successfully"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema creation → PostgreSQL | DDL 操作，创建表和索引 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S | JSONB 字段（存储复杂数据） | accept | JSONB 数据在应用层验证，PostgreSQL 只负责存储 |
| T-02-02 | I | SQL 注入（DDL 语句） | mitigate | 使用硬编码 SQL 字符串，不接受外部输入 |
| T-02-03 | D | 表删除（DROP TABLE） | accept | 使用 `CREATE TABLE IF NOT EXISTS`，不会删除现有数据 |
| T-02-04 | E | 索引缺失（性能下降） | mitigate | 所有表都创建 update_at 索引，role_account 额外创建查询索引 |
</threat_model>

<verification>
## Schema 创建验证

1. **表结构完整性检查**
   ```bash
   grep -c "CREATE TABLE IF NOT EXISTS role_" apps/role/internal/logic/role_schema.go
   ```
   预期：6（6 个表）

2. **索引创建检查**
   ```bash
   grep -c "CREATE INDEX IF NOT EXISTS idx_role_" apps/role/internal/logic/role_schema.go
   ```
   预期：7（6 个 update_at 索引 + 1 个 account_id 联合索引）

3. **字段类型正确性检查**
   ```bash
   grep -E "role_id.*BIGINT PRIMARY KEY" apps/role/internal/logic/role_schema.go | wc -l
   ```
   预期：6（所有表都有 role_id 主键）

4. **JSONB 使用检查**
   ```bash
   grep "JSONB" apps/role/internal/logic/role_schema.go
   ```
   预期：role_bag, role_activity, role_extra 使用 JSONB 存储复杂数据

5. **启动集成检查**
   ```bash
   grep -rn "InitRoleSchema" apps/role/
   ```
   预期：在 role app 启动流程中调用
</verification>

<success_criteria>
## 成功标准

1. **6 个表定义完整**
   - role_basic, role_bag, role_sign, role_activity, role_account, role_extra 全部定义
   - 每个表都有 role_id 主键（BIGINT）
   - 每个表都有 update_at 字段（TIMESTAMPTZ）

2. **索引创建正确**
   - 所有表都有 idx_role_*_update_at 索引
   - role_account 额外有 idx_role_account_account_server 联合索引

3. **字段映射准确**
   - MongoDB 字段正确映射到 PostgreSQL 类型
   - 复杂结构使用 JSONB 存储
   - 通用字段（role_id, update_at）在所有表中存在

4. **启动集成成功**
   - InitRoleSchema 在 role app 启动时调用
   - 表创建使用 IF NOT EXISTS，支持重复执行
</success_criteria>

<output>
After completion, create `.planning/phases/01-postgresql/01-02-SUMMARY.md`
</output>
