---
phase: 01-postgresql
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - apps/role/internal/logic/role_module.go
  - apps/role/internal/logic/role_basic.go
  - apps/role/internal/logic/role_bag.go
  - apps/role/internal/logic/role_sign.go
  - apps/role/internal/logic/role_activity.go
  - apps/role/internal/logic/role_account.go
  - apps/role/internal/logic/role_extra.go
autonomous: true
requirements:
  - INTF-01
  - INTF-02
  - INTF-03
user_setup: []

must_haves:
  truths:
    - "IPersistState 接口适配 PostgreSQL"
    - "GetIndexes() 返回 []string（索引字段名列表）"
    - "所有状态结构体使用 db 标签"
    - "移除 Version 字段（不需要乐观锁）"
  artifacts:
    - path: "apps/role/internal/logic/role_module.go"
      provides: "IPersistState 接口定义"
      min_lines: 50
      contains: "GetIndexes() []string"
    - path: "apps/role/internal/logic/role_basic.go"
      provides: "RoleBasicState 结构体（db 标签）"
      contains: 'db:"role_id"', 'db:"role_name"'
  key_links:
    - from: "role_module.go"
      to: "所有 role_*.go 文件"
      via: "IPersistState 接口定义"
      pattern: "type IPersistState interface"
    - from: "role_*.go"
      to: "PostgreSQL"
      via: "db 标签映射到数据库列"
      pattern: 'db:"'
---

<objective>
适配 IPersistState 接口到 PostgreSQL

目的：修改 `IPersistState` 接口和所有角色模块状态结构体，使其与 PostgreSQL 兼容，移除 MongoDB 特定功能（乐观锁）。

输出：
- 修改后的 `IPersistState` 接口（`GetIndexes()` 返回 `[]string`）
- 所有状态结构体使用 `db` 标签（替换 `bson` 标签）
- 移除 `Version` 字段（不再需要乐观锁）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-postgresql/01-CONTEXT.md

# 需要修改的文件
@apps/role/internal/logic/role_module.go
@apps/role/internal/logic/role_basic.go
@apps/role/internal/logic/role_bag.go
@apps/role/internal/logic/role_sign.go
@apps/role/internal/logic/role_activity.go
@apps/role/internal/logic/role_account.go
@apps/role/internal/logic/role_extra.go

# 依赖的上游计划
@.planning/phases/01-postgresql/01-01-PLAN.md
@.planning/phases/01-postgresql/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改 role_module.go 中的 IPersistState 接口</name>
  <files>apps/role/internal/logic/role_module.go</files>
  <read_first>
    apps/role/internal/logic/role_module.go - 查看当前 IPersistState 接口定义
    .planning/phases/01-postgresql/01-CONTEXT.md - 确认 D-06 决策
  </read_first>
  <action>
修改 `apps/role/internal/logic/role_module.go` 中的 `IPersistState` 接口和 `RolePersistState` 结构体，适配 PostgreSQL。

**修改内容：**

1. **修改 `IPersistState` 接口：**
   ```go
   type IPersistState interface {
       SetRoleID(roleID int64)
       GetUpdateAt() time.Time
       SetUpdateAt(updateAt time.Time)
       GetIndexes() []string  // D-06: 返回索引字段名列表（而非 mongo.IndexModel）
   }
   ```

2. **修改 `RolePersistState` 结构体：**
   ```go
   type RolePersistState struct {
       RoleID   int64     `db:"role_id"`
       UpdateAt time.Time `db:"update_at"`
       // 移除 Version 字段（D-08: 不再需要乐观锁）
   }
   ```

3. **移除 `GetVersion` 和 `SetVersion` 方法：**
   ```go
   // 删除这两个方法
   // func (r *RolePersistState) GetVersion() int64
   // func (r *RolePersistState) SetVersion(version int64)
   ```

4. **修改 `GetIndexes()` 方法实现：**
   ```go
   func (r *RolePersistState) GetIndexes() []string {
       return []string{"update_at"}
   }
   ```

5. **移除 MongoDB 相关导入：**
   ```go
   // 删除这些导入
   // "go.mongodb.org/mongo-driver/bson"
   // "go.mongodb.org/mongo-driver/mongo"
   // "go.mongodb.org/mongo-driver/mongo/options"
   ```

6. **保留的工具函数：**
   ```go
   func getColName(mod IPersistState) string {
       return gstr.CaseSnake(gxyutil.GetObjectName(mod))
   }
   ```

**完整修改后的 role_module.go：**
```go
package logic

import (
    "context"
    "gserver/core/gxymodule"
    "gserver/util"
    "time"

    "github.com/gogf/gf/v2/text/gstr"
)

type IRoleModule interface {
    gxymodule.IModule
    SetRole(role *RoleMain)
    OnCreate(ctx context.Context)
    PersistState() IPersistState // must return pointer
}

type IPersistState interface {
    SetRoleID(roleID int64)
    GetUpdateAt() time.Time
    SetUpdateAt(updateAt time.Time)
    GetIndexes() []string
}

type RolePersistState struct {
    RoleID   int64     `db:"role_id"`
    UpdateAt time.Time `db:"update_at"`
}

func (r *RolePersistState) SetRoleID(roleID int64) {
    r.RoleID = roleID
}

func (r *RolePersistState) GetUpdateAt() time.Time {
    return r.UpdateAt
}

func (r *RolePersistState) SetUpdateAt(updateAt time.Time) {
    r.UpdateAt = updateAt
}

func (r *RolePersistState) GetIndexes() []string {
    return []string{"update_at"}
}

func getColName(mod IPersistState) string {
    return gstr.CaseSnake(gxyutil.GetObjectName(mod))
}

type RoleModule struct {
    gxymodule.ModuleBase `db:"-"`
    RoleID               int64
    Role                 *RoleMain `db:"-"`
}

func (r *RoleModule) SetRole(role *RoleMain) {
    r.RoleID = role.RoleID
    r.Role = role
}

func (r *RoleModule) OnCreate(ctx context.Context) {
}

func (r *RoleModule) PersistState() IPersistState {
    return nil
}
```

**关键修改点：**
- 移除 `GetVersion()` 和 `SetVersion()` 方法（INTF-03）
- `GetIndexes()` 返回 `[]string` 而非 `[]mongo.IndexModel`（D-06）
- 结构体标签从 `bson:""` 改为 `db:""`（D-07）
- 移除 MongoDB 相关导入
  </action>
  <verify>
    <automated>grep -q 'GetIndexes() \[\]string' apps/role/internal/logic/role_module.go && ! grep -q "GetVersion\|SetVersion" apps/role/internal/logic/role_module.go && grep -q 'db:"role_id"' apps/role/internal/logic/role_module.go</automated>
  </verify>
  <done>
    - IPersistState 接口修改完成
    - GetIndexes() 返回 []string
    - RolePersistState 使用 db 标签
    - 移除 Version 字段和相关方法
    - 移除 MongoDB 导入
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改 role_basic.go 的结构体标签</name>
  <files>apps/role/internal/logic/role_basic.go</files>
  <read_first>
    apps/role/internal/logic/role_basic.go - 查看当前 RoleBasicState 结构
    apps/role/internal/logic/role_module.go - 确认新的 IPersistState 接口
  </read_first>
  <action>
修改 `apps/role/internal/logic/role_basic.go` 中的 `RoleBasicState` 结构体，将 `bson` 标签替换为 `db` 标签。

**修改内容：**

```go
type RoleBasicState struct {
    RolePersistState `db:"inline"`
    RoleName         string    `db:"role_name"`
    Head             string    `db:"head"`
    LoginTm          time.Time `db:"login_tm"`
    LogoutTm         time.Time `db:"logout_tm"`
    CreateTm         time.Time `db:"create_tm"`
    VipLv            int       `db:"vip_lv"`
}
```

**修改要点：**
- `RolePersistState` 嵌入使用 `db:"inline"`（对应 PostgreSQL 的列展开）
- 所有字段标签从 `bson:""` 改为 `db:""`
- 字段名保持不变（Snake case，与数据库列名一致）

**完整修改后的 role_basic.go 关键部分：**
```go
type RoleBasicState struct {
    RolePersistState `db:"inline"`
    RoleName         string    `db:"role_name"`
    Head             string    `db:"head"`
    LoginTm          time.Time `db:"login_tm"`
    LogoutTm         time.Time `db:"logout_tm"`
    CreateTm         time.Time `db:"create_tm"`
    VipLv            int       `db:"vip_lv"`
}

type RoleBasic struct {
    RoleModule
    RoleBasicState
}

var _ IRoleModule = (*RoleBasic)(nil)

func (r *RoleBasic) PersistState() IPersistState {
    return &r.RoleBasicState
}

// ... 业务方法保持不变 ...
```

**注意：**
- 业务逻辑方法（ReqBasicSetName, ReqBasicInfo 等）完全不需要修改
- 只修改结构体标签和嵌入标签
  </action>
  <verify>
    <automated>grep -q 'db:"role_name"\|db:"head"\|db:"login_tm"' apps/role/internal/logic/role_basic.go && ! grep -q 'bson:' apps/role/internal/logic/role_basic.go</automated>
  </verify>
  <done>
    - RoleBasicState 所有字段使用 db 标签
    - RolePersistState 嵌入使用 db:"inline"
    - 移除所有 bson 标签
  </done>
</task>

<task type="auto">
  <name>Task 3: 修改其他 role_*.go 文件的结构体标签</name>
  <files>
    apps/role/internal/logic/role_bag.go
    apps/role/internal/logic/role_sign.go
    apps/role/internal/logic/role_activity.go
    apps/role/internal/logic/role_account.go
    apps/role/internal/logic/role_extra.go
  </files>
  <read_first>
    apps/role/internal/logic/role_bag.go - RoleBagState 结构
    apps/role/internal/logic/role_sign.go - RoleSignState 结构
    apps/role/internal/logic/role_activity.go - RoleActivityPersistState 结构
  </read_first>
  <action>
修改剩余 5 个角色模块文件的结构体标签，将 `bson` 替换为 `db`。

**1. role_bag.go - RoleBagState：**
```go
type RoleBagState struct {
    RolePersistState `db:"inline"`
    Items            map[int]*bag.BagItem     `db:"items"`
    Currencies       map[int]*bag.BagCurrency `db:"currencies"`
    GridUse          int                      `db:"grid_use"`
}
```

**2. role_sign.go - RoleSignState：**
```go
type RoleSignState struct {
    RolePersistState `db:"inline"`
    DrawTime         time.Time `db:"sign_time"`
    SignDay          int       `db:"sign_day"`
    AccumDrawStage   []int     `db:"accum_draw_stage"`
    DrawDay          int       `db:"reward_day"`
    Patch            int       `db:"patch"`
}
```

**3. role_activity.go - RoleActivityPersistState：**
```go
type RoleActivityPersistState struct {
    RolePersistState `db:"inline"`
    Activitys        map[int32]server.ActivityData `db:"activitys"`
}
```

**4. role_account.go - RoleAccountState（如果存在）：**
```go
type RoleAccountState struct {
    RolePersistState `db:"inline"`
    AccountID        int64  `db:"account_id"`
    ServerID        int    `db:"server_id"`
}
```

**5. role_extra.go - RoleExtraState（如果存在）：**
```go
type RoleExtraState struct {
    RolePersistState `db:"inline"`
    ExtraData       map[string]interface{} `db:"extra_data"`
}
```

**修改规则：**
- 所有 `bson:""` 替换为 `db:""`
- `bson:"inline"` 替换为 `db:"inline"`
- 字段名保持不变（Snake case）
- 业务逻辑方法不需要修改

**批量验证脚本：**
```bash
# 检查是否还有 bson 标签
grep -rn 'bson:' apps/role/internal/logic/

# 检查是否所有文件都有 db 标签
grep -rn 'db:"' apps/role/internal/logic/role_*.go
```
  </action>
  <verify>
    <automated>! grep -rn 'bson:' apps/role/internal/logic/role_bag.go apps/role/internal/logic/role_sign.go apps/role/internal/logic/role_activity.go && grep -q 'db:"' apps/role/internal/logic/role_bag.go apps/role/internal/logic/role_sign.go apps/role/internal/logic/role_activity.go</automated>
  </verify>
  <done>
    - 所有 role_*.go 文件的结构体使用 db 标签
    - 移除所有 bson 标签
    - RolePersistState 嵌入使用 db:"inline"
    - 业务逻辑方法无需修改
  </done>
</task>

<task type="auto">
  <name>Task 4: 实现各模块的 GetIndexes() 方法</name>
  <files>
    apps/role/internal/logic/role_basic.go
    apps/role/internal/logic/role_bag.go
    apps/role/internal/logic/role_sign.go
    apps/role/internal/logic/role_activity.go
    apps/role/internal/logic/role_account.go
    apps/role/internal/logic/role_extra.go
  </files>
  <read_first>
    apps/role/internal/logic/role_module.go - 确认 GetIndexes() 方法签名
    .planning/phases/01-postgresql/01-CONTEXT.md - 确认 D-06 决策
  </read_first>
  <action>
为每个角色模块状态结构体实现 `GetIndexes()` 方法，返回需要创建索引的字段名列表。

**实现规则：**
- 基础模块（Basic, Bag, Sign, Activity, Extra）：只返回 `[]string{"update_at"}`
- role_account 模块：返回 `[]string{"update_at", "account_id", "server_id"}`（包含查询索引）

**1. role_basic.go：**
```go
func (r *RoleBasicState) GetIndexes() []string {
    return []string{"update_at"}
}
```

**2. role_bag.go：**
```go
func (r *RoleBagState) GetIndexes() []string {
    return []string{"update_at"}
}
```

**3. role_sign.go：**
```go
func (r *RoleSignState) GetIndexes() []string {
    return []string{"update_at"}
}
```

**4. role_activity.go：**
```go
func (r *RoleActivityPersistState) GetIndexes() []string {
    return []string{"update_at"}
}
```

**5. role_account.go（如果需要自定义索引）：**
```go
func (r *RoleAccountState) GetIndexes() []string {
    return []string{"update_at", "account_id", "server_id"}
}
```

**6. role_extra.go：**
```go
func (r *RoleExtraState) GetIndexes() []string {
    return []string{"update_at"}
}
```

**位置：** 将 `GetIndexes()` 方法添加在各自文件的结构体定义之后，业务方法之前。

**示例（role_basic.go）：**
```go
type RoleBasicState struct {
    RolePersistState `db:"inline"`
    RoleName         string    `db:"role_name"`
    // ...
}

func (r *RoleBasicState) GetIndexes() []string {
    return []string{"update_at"}
}

type RoleBasic struct {
    RoleModule
    RoleBasicState
}
// ... 业务方法 ...
```

**注意：**
- 如果某个模块没有自定义索引需求，可以不实现 `GetIndexes()`，会使用 `RolePersistState` 的默认实现
- Phase 2 会使用这些方法动态创建索引
  </action>
  <verify>
    <automated>grep -rn 'func.*GetIndexes()' apps/role/internal/logic/role_*.go | wc -l</automated>
  </verify>
  <done>
    - 所有状态结构体实现 GetIndexes() 方法
    - 返回值包含 update_at 索引
    - role_account 额外包含查询索引
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| struct tags → PostgreSQL | 结构体标签映射到数据库列名 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | 标签注入（db 标签） | accept | 标签是编译时常量，不接受外部输入 |
| T-03-02 | D | 数据丢失（Version 字段移除） | accept | Actor 单线程保证，不需要乐观锁 |
| T-03-03 | E | 接口不兼容（GetIndexes 返回类型变化） | mitigate | 统一修改所有实现，确保编译通过 |
</threat_model>

<verification>
## 接口适配验证

1. **IPersistState 接口检查**
   ```bash
   grep -A 5 "type IPersistState interface" apps/role/internal/logic/role_module.go
   ```
   预期：显示接口定义，包含 GetIndexes() []string

2. **标签替换完整性检查**
   ```bash
   grep -rn 'bson:' apps/role/internal/logic/role_*.go | wc -l
   ```
   预期：0（所有 bson 标签已移除）

   ```bash
   grep -rn 'db:"' apps/role/internal/logic/role_*.go | wc -l
   ```
   预期：> 30（所有字段都有 db 标签）

3. **Version 字段移除检查**
   ```bash
   grep -rn 'Version' apps/role/internal/logic/role_module.go
   ```
   预期：无结果（Version 相关代码已移除）

4. **GetIndexes 实现检查**
   ```bash
   grep -rn 'func.*GetIndexes()' apps/role/internal/logic/role_*.go
   ```
   预期：每个状态结构体都有实现（6 个文件）

5. **编译检查**
   ```bash
   go build ./apps/role/...
   ```
   预期：无编译错误
</verification>

<success_criteria>
## 成功标准

1. **IPersistState 接口适配成功**
   - GetIndexes() 返回 []string
   - 移除 GetVersion/SetVersion 方法
   - RolePersistState 使用 db 标签

2. **所有状态结构体标签更新**
   - 6 个 role_*.go 文件全部使用 db 标签
   - 移除所有 bson 标签
   - RolePersistState 嵌入使用 db:"inline"

3. **GetIndexes 方法实现**
   - 每个状态结构体都有 GetIndexes() 实现
   - 返回值包含 update_at 索引
   - role_account 包含额外查询索引

4. **业务逻辑零修改**
   - 所有业务方法（ReqBasic*, OnCreate* 等）保持不变
   - 只修改结构体标签和接口定义
</success_criteria>

<output>
After completion, create `.planning/phases/01-postgresql/01-03-SUMMARY.md`
</output>
