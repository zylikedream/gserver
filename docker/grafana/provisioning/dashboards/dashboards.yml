apiVersion: 1
providers:
  - name: gserver
    orgId: 1
    folder: GServer
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/dashboards
